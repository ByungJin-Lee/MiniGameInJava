package me.byungjin.network;

/**
 * 통신 규약 문서 참조
 */
public enum PROMISE {
	LOGIN(300), REGISTER(301), LOGIN_FAIL(302), LOGIN_SUC(303), REGISTER_FAIL(304),
	REGISTER_SUC(305), CUT_COMMU(306), 
	NICK(307), CHAT(308), GAME(309), RANK(310), RANK_RETURN(311), RANK_FAIL(340), RANK_UPDATE(341),
	ROOM(312), ROOM_END(313), ROOM_SUC(330), ROOM_FAIL(331), ROOM_LIST(332), ROOM_ITEM(333), ROOM_LIST_END(334), ROOM_ENTER(343), ROOM_EXIT(344), ROOM_ENTER_RETURN(345),
	READY(316), UNREADY(317), ROOM_START(350),
	VICTORY(318), LOSE(319), READY_TOO(320),
	
	/* Omok */
	PUT(303), PREDICT(304),
	
	/* Bulls And Cows */
	THROW(305), THROW_RESULT(306)
	
	, CUT_CONNECTION(307)
	;
	
	
	private short value;
	private PROMISE(int val) {
		value = (short)val;
	}
}
